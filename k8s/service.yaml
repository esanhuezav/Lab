apiVersion: v1
kind: Service
metadata:
  name: agenda-svc
  namespace: agenda-pvc
spec:
  type: NodePort
  selector:
    app: agenda
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30083